0.热门活动、参与的活动、我的活动都共同使用同一个activeList页面，但是要发送不同参数，在activeList页面的onload中发送不同请求 动态获得活动列表数据
1.上面的活动列表中点进去都是用同一个detail页面，根据活动简介点击的不同在detail的onload中发送不同参数，获得动态数据
3.个人信息提交后传到后台与保存，包括头像
4.活动创建、feedback提交后，后台接收反馈
5.活动检索
等等