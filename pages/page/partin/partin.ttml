<view tt:if="{{listData&&listData.length>0}}">
<view tt:for="{{listData}}" key="{{item.id}}" tt:for-index="idx">
<partin-activity-item  activity-name="{{item.activityName}}" going-on="{{item.goingOn}}"/>
</view>
</view>
<view tt:else>
您还没有参加过任何活动
</view>